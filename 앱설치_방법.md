# 📲 홈 화면 앱으로 설치하기

이 폴더의 **8개 파일을 모두** 같은 위치에 올리면, 폰에서 앱처럼 설치할 수 있습니다.
주소창 없이 전체화면으로 뜨고, 홈 화면에 아이콘이 생깁니다.

| 파일 | 역할 |
|---|---|
| diary.html | 앱 본체 |
| manifest.json | 앱 이름·아이콘·화면 방식 정의 |
| sw.js | 오프라인에서도 앱이 열리게 하는 캐시 |
| icon-192 / icon-512 / icon-maskable.png | 홈 화면 아이콘 |
| apple-touch-icon.png | 아이폰 홈 화면 아이콘 |
| favicon-32.png | 브라우저 탭 아이콘 |

> ⚠️ **전부 같은 폴더에 있어야 합니다.** 하나라도 빠지면 설치 버튼이 나오지 않습니다.

---

## 1단계 — 인터넷 주소로 올리기

PWA는 `https://` 주소에서만 설치됩니다. 파일을 직접 여는 방식(`file://`)에서는 안 됩니다.

이미 GitHub Pages를 쓰고 계시다면 **파일을 기존 폴더에 그대로 올리면 끝**입니다(기존 diary.html은 덮어쓰기).

처음이라면:
1. https://github.com 가입 → **＋ → New repository**
2. 이름 입력(예: `diary`), **Public** → Create repository
3. **uploading an existing file** → 파일 전부 끌어다 놓기 → Commit changes
4. **Settings → Pages** → Source **Deploy from a branch**, Branch **main / (root)** → Save
5. 1~2분 뒤 `https://아이디.github.io/diary/diary.html` 접속

---

## 2단계 — 설치하기

### 안드로이드 (크롬)
1. 폰 크롬으로 접속
2. 화면 아래 **"홈 화면에 추가하면 앱처럼 쓸 수 있어요"** 배너 → **설치**
3. 배너가 없으면 크롬 **⋮ → 앱 설치**

### 아이폰 (사파리)
사파리로 접속 → 하단 **공유(⬆️)** → **홈 화면에 추가**
(크롬이 아니라 **사파리**여야 합니다)

### PC (크롬·엣지)
주소창 오른쪽 끝 **설치 아이콘(⊕)** 클릭

---

## 설치하면 달라지는 것

- 홈 화면에 **주황 노트 아이콘**이 생깁니다
- **주소창·탭 없이 전체화면**으로 열립니다
- 인터넷이 끊겨도 앱 화면은 열립니다 (작성 내용은 연결되면 자동 저장)
- 앱 전환 목록에 별도 앱처럼 표시됩니다

---

## 자주 있는 문제

**설치 배너가 안 떠요**
- 파일이 모두 같은 폴더에 있는지 확인 (특히 `manifest.json`, `sw.js`)
- `https://` 주소인지 확인
- 이미 설치했거나 배너의 ✕를 눌러 닫았을 수 있습니다 → 브라우저 메뉴에서 직접 설치

**앱을 수정했는데 반영이 안 돼요**
새 `diary.html`을 올린 뒤 앱을 완전히 닫았다 다시 열면 최신본을 받아옵니다.

**아이콘이 이상하게 잘려요**
기종마다 원형·둥근사각형으로 다르게 자릅니다. `icon-maskable.png`가 그 경우를 위한 것이니 파일을 전부 올리면 정상 표시됩니다.
